[project]
name = "news-headlines-mcp"
version = "0.1.0"
description = "MCP tool that fetches top news headlines using the NewsAPI."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.3.0",
]

[tool.mcp]
entrypoint = "news_headlines.py"
